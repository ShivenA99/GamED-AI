"""V4 Algorithm Games Pipeline — generates interactive algorithm learning games.

5 game types: state_tracer, bug_hunter, algorithm_builder, complexity_analyzer, constraint_puzzle.
6-phase pipeline: Context → Concept → Plan → Content (parallel) → Assets (parallel) → Assembly.
"""
